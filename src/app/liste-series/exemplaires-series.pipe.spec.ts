import { ExemplairesSeriesPipe } from './exemplaires-series.pipe';

describe('ExemplairesSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExemplairesSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
