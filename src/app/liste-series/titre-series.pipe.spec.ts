import { TitreSeriesPipe } from './titre-series.pipe';

describe('TitreSeries.Pipe', () => {
  it('create an instance', () => {
    const pipe = new TitreSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
