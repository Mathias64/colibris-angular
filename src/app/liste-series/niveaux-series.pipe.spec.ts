import { NiveauxSeriesPipe } from './niveaux-series.pipe';

describe('NiveauxSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new NiveauxSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
