import { FiltreSeries } from './filtre-series.pipe';

describe('FiltreSeries.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreSeries();
    expect(pipe).toBeTruthy();
  });
});
